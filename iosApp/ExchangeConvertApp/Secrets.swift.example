import Foundation

// Template — copy to Secrets.swift and fill in your exchangerate.host access key.
// This file is committed; Secrets.swift is git-ignored.
enum ExchangeSecrets {
    /// exchangerate.host access key (currencylayer-compatible API)
    static let apiToken = ""

    /// API host used by the shared Ktor client
    static let apiHost = "api.exchangerate.host"
}
